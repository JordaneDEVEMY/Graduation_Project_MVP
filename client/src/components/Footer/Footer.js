import React from 'react';
import PropTypes from 'prop-types';
import './footer.scss';

function Footer() {
  return (
    <>

    </>
  );
}

Footer.propTypes = {
};
Footer.defaultProps = {
};
export default React.memo(Footer);
