import React from 'react';
import PropTypes from 'prop-types';
import './main.scss';

function Main() {
  return (
    <>

    </>
  );
}

Main.propTypes = {
};
Main.defaultProps = {
};
export default React.memo(Main);
