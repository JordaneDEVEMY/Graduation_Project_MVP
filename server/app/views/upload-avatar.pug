extends layouts/main.pug

block append main
    h1 Upload image :
    div(id="image-upload")
        form(method="POST" action="" enctype="multipart/form-data")
            div(class="file-input")
                label(for="file") image :
                input(type="file" name="image" id="file")
            button(type="submit" value="upload") Ajouter l'image
